# D13 工作台与独立浏览器

2026-09-19，源码实现完成。唯一写入者 01a0b572-9785-75c3-be63-b5e5d8bc6f4b。保留此前全部未提交改动，未覆盖正在登录的生产包。

实现：灰白工作台；城市搜索/多选/不限及紧凑筛选；四模块研究、链接候选正文读取与受限粘贴确认；JD 带入简历编辑；真实已读列表及独立收藏投递状态；发现内使用完整筛选/权重快照的定时任务；可拖动/键盘调整/持久化侧栏与右侧独立 WebContentsView。新增组件，App 从 371 行减少至约 235 行。空白数据库建立个人工作区，避免首次使用检索永远禁用。

## 验证

- `npm run build` 成功；Electron 24 tests passed。新增拒绝带凭据/非法端口 URL、非法 bounds 与小区域不扩张遮挡的边界用例。
- Python 研究输入、快照、研究和调度共 22 passed；API 9 passed，另一个启停测试首次被沙箱绑定回环端口限制阻挡，提权仅重跑该测试通过。1 个既有 Starlette deprecation warning。
- 真实 Electron + 生产 renderer，用户数据 `/private/tmp/jobfindsme-d13-user`。只导入合成简历。腾讯匿名只读搜索返回 58 条、3 个快照页；并非来源全量分页证明。
- 原生 CUA 验证：浏览器拖动 702→824，侧栏 275→219，网页在释放后恢复；键盘 Left 686→702；折叠与刷新后宽度/折叠状态保留。事件诊断修复 native lostpointercapture 早于最后 move/up 的序列，不再提前结束拖动。
- 实际窄屏 1094×862，明确切换工作区/岗位原页；工作区模式原生 view visible=false；关闭后 attached child view 数为 0。
- 最终主进程独立原生契约：腾讯 JD 提取 755 字，包含岗位职责/要求；bounds 700,80,500,680 → 620,80,580,680；返回/前进正确；close 不清会话，再开复用同一 webContents。网络存在部分子资源 TLS 错误，正文读取成功，未绕过证书。
- 手工粘贴 JD → 确认 → 事实准备报告成功，不调用模型。多城市任务真实保存上海/深圳、薪资策略、阅读条件及简历/规则版本，随后暂停。
- 截图与 JSON 证据位于 [历史提交的 D13 目录](https://github.com/russeell/jobfindsme/tree/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D13)。BrowserWindow.capturePage 不包含原生子视图，因此原生视觉证据使用 CUA 截图，不用空占位截图冒充网页可见。

## 限制

未进行真实登录/投递、未使用付费模型、未替换旧生产运行包。三登录平台 D01/D04 的验收仍保留。JD 自动提取不能保证所有平台页面成功，失败明确提示粘贴并标记用户确认、未独立核验。

用户随后追加 D14（模型预设、本地免密钥、来源简化/官网人工浏览、品牌图标、默认 10 条及固定滚动、浏览器开关），继续由同一会话实施。
