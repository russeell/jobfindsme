# D06 验收证据

日期：2026-09-18。差异：当前 `codex/desktop-refactor` 未提交工作树，实施会话 `01a0b4ea-f5a3-7b50-937f-27bb5f4f144f`。

## 结果

- 展示、阅读、收藏、投递分离：`job_read_events` 只接受显式 read/unread，`job_tracking_flags/events` 独立保存 saved/applied/apply_opened。收藏和已投递可并存且可各自撤销。
- 0020 仅把旧 `job_states` 的 saved/applied 迁入对应状态；旧 `search_job_impressions` 没有迁入阅读事件。
- 岗位详情提供“岗位研究”入口，明确标记 D10 待接入且不生成虚构报告；“打开原页投递”导航到同来源隔离 WebContentsView，远程页仍无 Node/preload 权限。
- 打开原页只记录 `apply_opened`，不会写入 applied；用户在平台完成后需显式点击“确认已投递”。开发未提交任何申请。
- 新增求职记录页，只列出有显式阅读/收藏/投递操作的岗位。

## 验证

- Python 状态转换与迁移集成样例：原页打开后 applied 仍为 false；read/saved/applied 可同时为 true；撤销 saved 不影响其他两项；重放 0020 时旧 saved 保留而旧 impression 不生成 read。包含在 D05/D06 联合 `11 passed` 中。
- `npm test` → `12 passed`，其中隔离原页样例验证同来源 HTTPS 岗位 URL 会导航、重复 URL 不重载、跨域 URL 被拒绝；该测试不实际投递。
- 本地预览人工检查求职记录空状态与 desktop-v3 导航/留白。

## 限制

本轮不使用真实账号、不做真实投递；原页导航用脱离登录的白名单隔离样例验收。D01/D04 仍负责真实平台登录、详情和投递页兼容性，D12 依赖该最终关卡。
