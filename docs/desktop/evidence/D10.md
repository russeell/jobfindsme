# D10 验收证据

日期：2026-09-18。差异：当前 `codex/desktop-refactor` 未提交工作树，实施会话 `01a0b4fc-0b95-7da2-b969-5a13a5ae0a54`。

## 结果

- 新增岗位研究报告和证据表；每份报告绑定岗位与已确认简历版本，分开 JD 事实、简历对照、项目改写方向、面试准备和公司/团队评价证据。
- `WebEvidenceSearch` 通过公开 RSS 搜索面检索脉脉、OfferShow、看准和知乎，只保存目标站点原始 HTTP(S) 链接，不保存通用搜索页。公开来源受限、无结果、无可核验发布日期均有显式状态/限制。
- 用户可提供链接或摘录，但永久标记 `user_supplied_unverified`；无证据时报告明确拒绝生成公司好坏结论。
- 可选模型增强只在用户选择已验证且带系统安全存储 Key 的连接时调用；发送载荷绑定简历版本、排除基本信息并再脱敏，JD/网页摘录被显式包装为不可信数据；结果经结构校验，支持取消。未选连接时不读 Key、不请求模型。
- desktop-v3 “岗位研究”导航与岗位详情入口已接通；保留左侧导航、灰白配色、卡片留白及右侧报告工作区，提供项目简历改写入口。

## 验证

- `uv run pytest tests/desktop_api/test_research.py tests/desktop_api/test_scheduler.py tests/models/test_gateway.py tests/resume_editor/test_prompt.py -q` → `16 passed`。其中研究用例覆盖原始链接/发布日期/团队相关性映射、用户摘录未独立核验标记、无证据不下结论、不可信载荷与模型结构校验。
- `uv run ruff check ...` → passed。
- 真实公开来源单次检查：`WebEvidenceSearch(company="OpenAI", source_ids=("zhihu",))` → `{'zhihu': 'available'}`，返回原始链接 `https://www.zhihu.com/question/572946129`；RSS 日期为非标准本地化格式，因此 `published_at=None` 并按约定显示“日期不可核验”，未猜测日期。
- `npm run build` 通过联合 TypeScript 检查和 Vite 构建；`npm test` → `12 passed`。
- 本地 Electron 原生窗口冒烟：“岗位研究”导航可用，左侧证据/模型选择表单与右侧报告工作区存在，未注入演示岗位或真实简历。

## 限制

脉脉/OfferShow 等社区页面可能要求登录或拒绝公开索引；本任务将其报告为受限，不把无结果写成“没有负面评价”。本轮没有使用用户 API Key 或发起付费模型请求。
