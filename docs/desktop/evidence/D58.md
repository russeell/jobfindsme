# D58 · GitHub 文件树整理

2026-09-25：保留运行源码、数据库迁移、测试、构建配置、面向用户的 README 图片和简短任务验收记录；从当前 GitHub 文件树移出 287 个不参与运行或测试的历史截图、日志、JSON、一次性脚本、旧方案和自动生成报告。`docs/` 由约 22 MB 降至约 1.4 MB。旧附件可通过验收文档内的固定提交链接查阅；本机副本在忽略的 `.development-archive/2026-09-25-github-document-cleanup/`，附 SHA-256 清单，287 个文件校验全部一致。没有重写 Git 历史。

文档相对链接与任务证据路径已检查，README 插图移至 `docs/images/`。`.gitignore` 阻止同类原始附件和自动生成报告再次进入 Git，文字验收记录仍可跟踪。

验证：桌面构建通过；Node 测试 83/83；Python 测试 483/483；`scripts/sync_skill.py --check` 与 `git diff --check` 通过。仓库目录边界见 [项目结构](../STRUCTURE.md)。
