# D12b 本地生产包主流程与打包缺陷修复

日期：2026-09-19。状态：done；D12 最终移除旧 MCP、签名、公证与发布仍未开始。

## 真实生产包主流程

- 从 `apps/desktop/release/mac-unpacked/JobFindsMe.app` 启动实际 macOS arm64 应用，使用 `/private/tmp/jobfindsme-packaged-smoke3.CAzJgZ` 作为全新隔离用户数据目录。
- 导入只含虚构姓名/邮箱/电话/经历的合成 Markdown 简历；解析 4 项事实，确认生成 v1，编辑技能并保存 v2，历史版本仍可见。
- 分析副本预览成功且不含合成 PII；Markdown 导出成功至 `/private/tmp/简历-v2064b0.md`，文件权限 `0600`，内容与 v2 一致。
- 同一 `.app` 完成腾讯 58 条/3 页和猎聘 92 条/5 页生产检索、跨页、详情及官方原页只读冒烟；未执行真实申请。

## 生产缺陷与修复

- Electron Framework 的相对符号链接被复制为绝对路径，导致 `icudtl.dat` 缺失和应用崩溃：打包复制启用 `verbatimSymlinks`；审计拒绝逃逸符号链接并验证 Framework 必需资源。
- PyInstaller 单文件可执行程序被错误追加 `-m jobfindsme.desktop_api`：生产启动不再传模块参数，开发 Python 仍保持 `-m`。
- 单文件冷启动实际约 16–20 秒，原 10 秒超时产生假失败：生产就绪预算提高到 60 秒，开发仍为 10 秒。
- 简历导出把路径参数 `version_id` 重复放进严格请求体，FastAPI 返回 422 且 UI 显示 `[object Object]`：请求体改为显式字段，验证错误数组转为可读消息。
- 腾讯来源被错误路由到仅供登录平台的 Electron 桥：只有 BOSS/智联/前程无忧走浏览器搜索，腾讯走 Python 官方公开适配器；发现岗位和定时任务均显示真实可用来源。

## 验证

- 最终 Electron 23 tests passed；包含生产/开发 Python 启动参数、导出严格请求体、固定来源动作、同源导航完成/超时、验证能力汇总和门禁契约。
- `npm run pack:mac:dir` 与 `npm run audit:mac:dir` 通过；最终产物仍为本地未签名目录包，未发布。
- 实测导出的合成简历和隔离数据库仅存在临时目录，不进入应用产物；package audit 继续排除数据库、WAL、简历、`.env`、凭据和浏览器会话。
- 关键 SHA-256：`python-service.ts` `f8629887...9627`；`api-client.ts` `4635bdab...9464`；`package-macos.mjs` `7e6064e6...4f12`；`audit-macos-package.mjs` `046649b3...1c79`。

## 限制

最终重建后调度者已再次验证 `.app` 正常启动、来源页和 BOSS 隔离登录页可达；没有完成登录。职位原页的 SPA 完成态判定仍由契约测试覆盖。D12 仍依赖 D04/D01 的三个真实登录来源验收，且不得提前删除旧 MCP/skill、签名、公证、推送或发布。
