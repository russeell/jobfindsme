# D17 浏览器布局、多标签与单实例（安全交接）

2026-09-19；未提交工作树。功能和原生退出回归均已完成。

## 可复现问题与修复

1. 默认打开腾讯：D15 Workbench.toggle在无目标时硬编码company_01。改为无预选来源的新标签空态；明确选择来源或链接后才打开。
2. 默认窗口不能右侧并排：D15断点为侧栏宽度+1060，默认1240窗口落入窄屏替换模式。改为360主区+520浏览器预算，空间不足先自动折叠侧栏，再显式工作区/原页切换；根grid显式一条视口高度行。未在当前D15只读现场复现用户所说“上下堆叠”，不以推断当已证实根因；新包以原生bounds和截图证明不追加到文档流。
3. 同来源页面覆盖：旧实现每来源只有一个前台view。改为每标签独立WebContentsView、URL/前后历史，按相同当前URL复用，关闭后切邻近标签；标题/加载态、最多12个标签。平台内仍共享原persist partition，后台检索独立view不抢前台导航。白名单window.open/target=_blank路由新标签，其他地址拒绝并提示。
4. 重复实例：启动最早获取Electron按userData的single-instance lock，第二次启动聚焦原窗口，早于Python/SQLite/调度启动。独立目录可并存。
5. 退出报错：真实多标签退出时closed回调仍调用已销毁窗口getContentSize，弹Object has been destroyed。已改为直接清理所有webContents，销毁期间不切换相邻标签、不读取原生布局；补充销毁窗口离线回归，并完成修复后的“有网页标签退出”原生回归。

## 实际验收

- 中间D17真实包，默认窗口1240×820（内容1240×788）：主区x219/w360/h788；右侧面板x579/w661/h788；活动原生view x580/y136/w660/h652，只有一个活动view可见。保留旧本机偏好侧栏219、浏览器900仍正确收敛。见 `D17/default-layout.json`、`D17/right-browser.png`。
- 真实腾讯详情→社会招聘在首标签产生独立历史；新标签打开腾讯详情，首标签后退时第二标签不变；第三标签打开字节官网；关闭字节回退腾讯。猎聘首页一次返回about:blank，没有视作成功，后补“未返回有效内容”提示。
- 收起侧栏、浏览器放大/还原均不改变当前页面；收起浏览器原生view.visible=false，恢复原标签。窄屏893×719内容区：显式42px模式栏，浏览器x219/y42/h677，原生view x220/y178/w673/h541；切回工作区全部原生view隐藏。见 `D17/narrow-layout.json`、`D17/narrow-browser.png`。
- 单次腾讯公开检索57条，10/页；长列表scrollTop200/scrollHeight1219/clientHeight192，右侧面板仍y0/h788，document高度=viewport788，无纵向追加。见 `D17/scroll-layout.json`、`D17/list-scroll.png`。
- 同目录二次启动退出、原后端PID不变、首窗口聚焦；另一隔离目录可运行，见 `D17/single-instance.json`。
- 桌面29测试通过（含独立历史/复用/共享分区/后台隔离、popup安全/标签上限、窗口销毁回归）；类型构建、包审计通过。D16后端四维6项、快照与调度8项通过；相关ruff和git diff --check通过。

## 产物与隔离

最终可打开：[JobFindsMe-D17-Preview.app（已归档）](../../../.development-archive/2026-09-19-d20/tmp/JobFindsMe-D17-Preview.app)。标识D17-20260919，名称“JobFindsMe D17 测试版”，bundleID `com.jobfindsme.desktop.preview.d17`。
包内jobfindsmePreview=true，自身设置默认userData=`~/Library/Application Support/jobfindsme-preview-D17`，不依赖shell参数。无user-data-dir参数启动已通过实际读取验证，见 `D17/preview-isolation.json`；此最终包无标签关闭正常退出。不要把中间 [JobFindsMe-D17.app（已归档）](../../../.development-archive/2026-09-19-d20/tmp/JobFindsMe-D17.app) 当交付。

最终主进程SHA256 `af87f77ced2ff3a3136f58b4d30d4e0ac403c28ee4373a631617764cccbb7a62`；renderer index-DO4MZAvO.js SHA256 `326357ac28b3debc7172188fb9b1c9e088fa73f852d5f0a904e0a6ff70139c03`。

## 验收工具异常与影响

用户当前D15进程经只读确认来自 [JobFindsMe-D15.app（已归档）](../../../.development-archive/2026-09-19-d20/tmp/JobFindsMe-D15.app)，正式userData为jobfindsme-desktop。本轮未关闭或覆盖它。
关闭旧D17隔离测试的退出错误提示后，CUA路径重选自动重新启动D17默认profile（PID56785，02:46:47）。第一次SIGTERM被自动审批拒绝，未执行；补查空窗口无表单、PID/时间/可执行路径/独有Python56807、业务DB时间后，同一SIGTERM获批执行。没有绕过拒绝或按名称批量关进程。
只读正式DB：最后业务搜索2026-09-18T18:05:28Z，早于误启动18:46Z；定时任务/运行0、简历版本0、报告0，DB mtime未因该次启动变化。存在Chromium的Singleton锁/blob元数据写入；未清会话、未回滚或删除数据库。随后采用固定隔离的最终Preview包。
最终CUA getApp调用等待约11056秒才返回，期间调度要求安全交接。此后未再发CUA动作，最终测试进程通过原测试driver正常关闭（50587 exit0）。

# Final native exit verification

- Rebuilt the isolated `JobFindsMe D17 测试版` package from the final source.
- Launched the package without a `--user-data-dir`, opened one Tencent native browser tab, and closed the application through Playwright/Electron. The run returned `{"openedTabs":1,"exited":true}` with no destroyed-content-view error.
