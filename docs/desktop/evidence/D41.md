# D39–D41 验收记录

日期：2026-09-23。交付版本：`D41-20260923-ui31`。

## 简历维护（D39）

- 主入口收敛为导入、逐条核对、确认当前版本、手动维护、历史恢复与 PDF/DOCX/MD 导出。主页面不再展示对话补丁、模型选择、按 JD 改写；历史版本和原始文件仍保留。
- `deterministic-resume-v4` 识别同一行的中文段落标题。UI30 隔离包导入 6 行 TXT 样本，得到 2 条技能、1 条教育、1 条工作、1 条项目事实；姓名未当作事实，技能行未混进项目。
- 原生窗口确认导入生成 v1，修改项目经历生成 v2，从历史恢复 v1 生成 v3。原 v1/v2 仍可见，当前 v3 内容与 v1 一致。UI31 沿用隔离样本，当前简历在[默认宽度](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D41/resume-default-ui31.jpg)和[约 860px 窄窗](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D41/resume-narrow-ui31.jpg)均有内容。实际通过原生保存对话框导出 v3 的 [MD 文件](D41/resume-v3-export.md)，核对教育、工作、项目、技能与 v3 一致，且不包含 v2 的编辑内容。PDF/DOCX 按钮仍可见；其文件签名和内容导出沿用 [D35 验证](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D35/verification.json)，本轮未重新导出。

## 岗位研究（D40）

- 自定义问题先于快捷方向用于有限公开检索，并存入报告岗位快照；无自定义问题时使用快捷方向。结果先显示已保存报告，调整问题与再次研究折叠在后。粘贴链接解析后先确认岗位并设置问题，再发起研究。
- 修复了历史报告读取时将已保存的空快捷方向 `[]` 错误恢复为四个默认方向的问题。缺失或 JSON `null` 仍沿用默认方向。14 项研究测试通过，包括创建、重新读取、列表读取均保留 `[]` 与原始问题。
- UI31 在隔离工作空间通过本地接口创建一份明确标注为合成样本的报告，来源列表为空，没有发起外网口碑搜索。切换页面后从历史列表重新打开，[报告正文](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D41/research-saved-ui31.jpg)仍显示原始问题、快捷方向“未选择”和“用户提供的摘录（未核实）”。该摘录不代表真实雇主口碑；真实用户岗位的外网报告未验证。

## 来源与智联登录（D41）

- 已在 Google Chrome 同一官方登录链接确认手机号、短信验证码表单。UI23 内嵌页最初只显示宣传图和页脚。临时诊断记录到 `XMLHttpRequest.setRequestHeader` 的非 ISO-8859-1 字符错误，同时确认 Electron 默认 User-Agent 含中文应用名；将智联前台原页 User-Agent 规范为 ASCII 后，冷启动表单恢复。请求头的具体站点脚本未进一步逆向；临时日志未进入 UI30。
- UI29 冷启动的全新隔离资料目录里，点“岗位来源 → 智联招聘 → 打开来源/登录”直达用户提供的 `passport.zhaopin.com/login?bkUrl=...`，原生无障碍树立即出现手机号、短信验证码、获取验证码、协议勾选和登录按钮。UI31 同代码在隔离资料目录再次核对并保存[表单画面](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D41/zhilian-form-ui31.jpg)。没有输入账号或验证码，因此登录会话、列表、详情和网站续页均未验证。
- [来源卡片](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D41/source-cards-ui31.jpg)默认窗口为 2 列，约 860px 窄窗为 1 列；移除了重复的检索状态文案。登录、列表、详情和网站续页状态分开记录。已加载的智联/前程无忧页面只做被动 DOM 观察；看到可读列表后才允许一次有冷却期的有界检索。安全验证立即停源。四平台与 16 家公司各自能力和历史证据见 [来源矩阵](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D41/source-matrix.json)。矩阵保留 D37/D38 的有界样本日期，未把历史样本写成当天重新核验。
- 发现页来源采集改为最多两个来源同时运行，同来源后台检索串行；界面展示首个来源完成时间、各来源耗时和候选数。受阻来源保持部分结果与限制说明。

## 验证与边界

- UI30 阶段的 Python 定向测试 17 项、桌面来源定向 Node 测试 34 项通过。UI31 新增修复后，研究测试 14 项、ruff、前端类型检查与构建、Python runtime 重建、两份 `.app` 包审计通过。
- UI31 原生窗口检查覆盖简历默认/窄窗、来源卡片、智联真实表单和本地合成报告历史。口碑报告的真实外网采集、智联登录后的自动搜索和 20 来源当天全量实测仍待各自有界验证。
- 用户 D38 测试版与既有会话保持运行；隔离包 `/private/tmp/JobFindsMe-D41-UI31-Isolated.app` 沿用隔离资料目录 `jobfindsme-preview-D41-qa30`。交付包 `/private/tmp/JobFindsMe-D41-UI31.app` 沿用既有默认用户资料目录，未启动它以免触碰用户会话。
