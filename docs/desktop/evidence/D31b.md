# D31b 真实会话阶段验收

2026-09-19，用户确认“已登陆”后，以原生 UI 检查实际运行的 `D32-20260919-ui16`。未读取或导出 cookies，未重启窗口，未重新发起完整搜索。

- 实际 BOSS 详情原页显示消息和简历导航，确认当前已登录。
- 发现页已有用户执行的 `agent` 检索结果；来源覆盖显示 BOSS 3 采集批次、部分结果、暂未发现新增。未把这一状态当成滚动新增验收通过。
- 当前选中 Agent开发工程师（原页公司为上海瑞汀灵动科技）。点击“读取完整 JD”后，应用显示完整岗位职责和任职要求，含读取时间 2026/9/19 16:53:02；与右侧原页所示内容一致。
- 发现实际字段缺陷：列表标题混入私用区薪资字符，公司显示为 BOSS直聘，地点未知。源代码确认列表选择器回退整个链接文字、空公司被 normalizer 回退为来源名；补 JD 时 Discovery 只合并 description，未使用详情 title/company。这些是待修复问题，不能将真实来源完整验收标 done。
- 未点击沟通、投递、发送消息或调用付费模型。未修改任何 .app 产物。停止/风险暂停及新增去重仍未获此次真实验收证据。

## 字段修复与离线复验

- 列表不再以整个岗位链接回退标题；按专用名称节点读取，删除薪资/标签子节点，无法区分的字段不猜测。卡片优先使用外层 wrapper，避免公司/地点在内层链接之外被漏读。
- 来源名称不再填入公司；未知公司参与指纹时附加岗位URL，避免不同公司的未知岗位被合并。历史BOSS标题中的PUA薪资尾部和平台名伪公司在读取时修正显示，岗位ID不变。
- 完整详情将title/company/location/salary/JD传入鉴权API，仅允许同工作区、同BOSS岗位链接补全，保持岗位ID与记录并持久化。解析不到的详情字段保留已有可靠字段。
- `D34/verify-ui.cjs`：原生DOM fixture覆盖嵌套岗位链接、外层公司地点、PUA薪资、无专用标题节点；3个岗位先采集2个详情，再对第3个调用实际IPC/API补全，读取数据库确认标题、公司、上海、25–35K、JD保存成功。全部网络为离线样例。
- 最终包 UI17，不覆盖UI16。当前用户真实实例仍运行UI16；新解析器尚未在真实已登录页面复验。保留blocked，待正常切到UI17后仅一次小样本；不要求重复登录。滚动新增/真实风险暂停仍无完整证据，不将D01/D04标done。

## D37 补验完成

2026-09-19 在用户确认的持久会话中完成一次有界 BOSS 检索：返回 15 条岗位、2 个实际桌面采集批次，并成功读取 1 条完整 JD。列表标题、公司和 JD 补全使用修复后的链路；同请求合并、取消、部分结果与风险暂停由定向契约测试覆盖。没有关闭用户实例、导出会话或触发投递。D31b 因此完成；智联和前程无忧的独立缺口仍使 D01/D04 保持未完成。详情见 [D37](D37.md)。
