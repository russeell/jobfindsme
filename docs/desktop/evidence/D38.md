# D38 来源浏览器兜底修复（进行中）

日期：2026-09-20；版本：`D38-20260920-ui22`。

- 京东官网独立会话使用“工程师 / 北京”实读 2 个网站页、17 条去重岗位；岗位详情 `requementId=225111` 读取完整 JD 457 字。官网列表为有界覆盖，未知字段保留。
- 前程无忧现有持久会话仍处于登录状态。真实搜索页加载“工程师”岗位卡片及网站分页控件；官网脚本确认岗位标题通过 `window.open('_blank')` 后设置子窗口地址。生产解析补充 `.joblist-item`、`.jname`、`.sal`、`.area`、`.cname` 与 `.el-pagination .btn-next`，不触碰投递按钮。
- 原生 Electron 回归 `ELECTRON_RUN_AS_NODE= ... verify-contracts.cjs` 通过：子窗口采用 Electron 传入的预创建 WebContents，保留 WindowProxy；真实岗位地址进入应用标签；未触发投递。此前直接新建 WebContentsView 会触发 `Invalid webContents`，已更正。
- 定向 Node 测试 17 项通过；桌面构建通过。城市条件在公开读取与内嵌浏览器兜底间一致传递，平台翻页中断保留已读取页，公司官网分页指纹使用规范化岗位 URL。

限制：阿里岗位列表已能经实际点击得到链接，但详情页资源加载失败，仍不能标记完整 JD 已验证；智联登录状态失效，不重复尝试。D38 保持进行中，继续补足可自主验证的网站翻页与来源矩阵。
