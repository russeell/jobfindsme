# D49 实施边界（2026-09-24）

用户确认主线为找机会 → 看岗位 → 调查公司 → 自主投递。D49 按以下顺序验收：

1. 历史简历与规则只从普通列表移除。已有检索、研究、旧计划的引用快照仍可按 ID 读取；当前版本不能移除；旧数据库通过最小迁移升级。
2. 从导航及普通流程移除匹配规则页、权重和评分模式控件；内部规则数据与历史结果兼容保留。岗位卡片和详情以可核对的技能、经验、薪资、JD、来源和时间说明代替数字分数。默认不会调用付费模型。
3. 简历页保留上传、核对、确认及明确的求职偏好；城市、岗位、薪资偏好独立存储，用户保存后参与下次检索请求和过滤。清除当前简历后，无简历关键词检索仍可用，旧快照保留；未确认导入不得参与检索。
4. 检查现有去重、详情缺失、发布日期和部分失败保留逻辑，只修复可复现缺陷。研究沿用公司/岗位两方向，核对报告保存、重开、更新与证据来源；不新增外部代理或付费调用。
5. 用合成数据覆盖以上状态迁移和主流程；独立应用包做默认/窄窗截图与审计。真实招聘来源仍按登录和风控门禁报告，不做批量线上检索。

保留真实用户数据与旧版运行实例，不推送、发布、自动投递或恢复定时检索。README 主图保留并标注旧导航；旧 D48 验收截图作为历史事实保留。
