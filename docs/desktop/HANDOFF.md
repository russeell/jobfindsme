# 当前交接

2026-09-23：D39–D41 已完成，验收和未验证边界见 [D41 记录](evidence/D41.md) 与 [20 来源矩阵](evidence/D41/source-matrix.json)。当前待续任务为 D38，状态仍是 blocked；来源账号/验证码、智联登录后的检索能力、阿里详情资源等不可从登录页推定。任务状态以 [tasks.json](tasks.json) 为准。

- 交付独立包：`/private/tmp/JobFindsMe-D41-UI30.app`，版本 `D41-20260923-ui30`。它沿用既有默认用户资料目录，未启动，避免触碰用户正在使用的 D38 会话。UI30 隔离包单独使用 `jobfindsme-preview-D41-qa30`。
- 简历主流程是导入、核对、维护当前版本、恢复历史和导出；UI30 原生样本验证 v1 → 编辑 v2 → 从 v1 恢复为 v3。口碑调查支持自定义问题，先确认解析岗位再设置问题；历史报告仍可打开。
- 智联官方登录链接在隔离包冷启动可见手机号和验证码表单。修复限于前台智联原页 User-Agent 中的非 ASCII 应用名，保留网页安全设置。表单可见不等于已登录，也不等于自动检索成功。
- 来源状态分层并按页面被动观察；看到列表才做一次有界探测，风险验证停源。来源目录仍为 4 平台 + 16 公司；D37/D38 历史样本在矩阵中标注日期，未做当天全来源重新实测。
- 定向验证：17 项 Python、34 项 Node、ruff、前端构建、Python runtime 构建与两份包审计通过。D38 旧包及用户会话未关闭、覆盖、复制；没有推送、发布、真实投递或付费模型调用。

此前 D38 已实测京东官网 2 个网站页、17 条去重岗位及一条 457 字 JD；前程无忧会话可见列表及分页控件；阿里列表点击可取得链接但详情资源失败。详情见 [D38 记录](evidence/D38.md)。
