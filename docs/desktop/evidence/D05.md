# D05 验收证据

日期：2026-09-18。差异：当前 `codex/desktop-refactor` 未提交工作树，实施会话 `01a0b4ea-f5a3-7b50-937f-27bb5f4f144f`。

## 结果

- 新增 `desktop_search_runs` / `scoring_rule_versions` 迁移与 `DesktopJobService`；每次搜索绑定当前已确认简历版本、权重规则版本、筛选快照与有序岗位 ID。
- 城市、薪资区间（默认交集/严格包含）、校招/社招、用工类型、经验、来源、已看均进入确定性硬过滤；选定字段的未知值支持包含/排除/仅未知三态。
- 权重键固定为职责/技能/项目/加分项，合计非 100 直接拒绝；存在当前简历时，该版本的技能和项目一定进入评分。
- 分页仅切不可变有序 ID 快照，默认 20，可选 10/20/50；同分用 `job_id` 稳定破平。
- desktop-v3 视觉人工检查：原导航分组、灰白配色、卡片留白、列表/右侧详情保留；短列表不会把详情压到末尾，880px 以下由“列表/详情”切换代替堆叠。

## 验证

- `UV_CACHE_DIR=/tmp/jobfindsme-uv-cache uv run pytest tests/desktop_api/test_job_snapshots.py tests/desktop_api/test_source_search.py -q` → `11 passed`（仅 Starlette 第三方弃用 warning）。55 条样例跨 3 页无重复/遗漏，快照后插入新岗位不扰动第 1 页。
- `uv run ruff check ...` → passed。
- `npm run build` → 联合 TypeScript 检查及 Vite 生产构建通过。
- 本地 Vite 预览人工查看发现页和求职记录页；预览无 desktop preload，未注入演示岗位。

## 限制

D01/D04 真实平台完整验收仍未完成；本任务只对已通过来源门禁的记录创建快照，没有启用任何未验证来源。
