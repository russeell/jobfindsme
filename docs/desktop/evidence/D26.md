# D26 灰白主题收拢（2026-09-19）

用户反馈 UI3 配色回退。只读进程和实际窗口确认当前运行包为 UI9，实际主界面为灰白，未发现整套主题回退；不能据此断言用户指的 UI3 没有问题。已询问具体指当前窗口还是旧预览，尚未收到回答。

确认的局部不一致：浏览器底色与其他表面不共用变量，警告使用大面积黄/红底，权重条残留蓝灰，原生控件未固定色系。新增最后加载的 `renderer/src/theme.css`，统一这些表面、固定 light color-scheme 和深灰 accent-color，窗口启动底色改白色。保留状态文字语义与链接下划线。

验证：`npm run build --prefix apps/desktop`（含两个 TypeScript 检查）通过；`JFM_PACKAGE_OUTPUT=/private/tmp/JobFindsMe-D25-UI10.app node apps/desktop/scripts/audit-macos-package.mjs` 通过。使用原生 CUA 在 UI10 隔离窗口查看发现岗位、岗位研究入口、岗位来源，白色工作区/浅灰侧栏/深灰勾选框正常。未运行无关后端测试。

包：`/private/tmp/JobFindsMe-D25-UI10.app`，默认隔离 profile；未覆盖运行中的 UI9 或迁移真实数据。该包也含此前 D25 修改，D25 的完整原生网页遮挡验收仍未完成，任务状态保持 in_progress，不能将本次主题检查视为 D25 全部通过。
