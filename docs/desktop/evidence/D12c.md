# D12c 桌面导航、来源说明与启动状态收尾

日期：2026-09-19。状态：done（源码与最小检查）；按协调要求未替换当前等待用户登录的运行中 `.app`。

## 实现

- 侧栏“匹配规则”不再 disabled/显示“待接入”，进入与 `desktop-v3` 同结构的规则页面：均衡、技能优先、项目优先模板，四项权重、合计 100% 门禁、规则预览和评分边界。
- 匹配权重提升到 `App` 共享状态；规则页应用后，发现岗位页和下一次 `runSourceSearch` 使用同一组权重，不是独立演示控件。发现页原有折叠编辑仍可用。
- renderer 不再在挂载时无条件抢跑 bootstrap。它先读取本地服务状态，只在 connected 后加载；连接事件也会触发加载，既不会因启动竞态锁死，也不会把 `desktop API is not ready` 原始实现错误闪给用户。
- macOS `activate` 在 Python 运行时尚未就绪时不再提前创建窗口；正常启动仍在服务 ready 后创建窗口。
- 猎聘目录说明更新为证据分层文案：92 条是 2026-09-19 生产 `.app` 搜索快照；5 页是该快照的桌面结果分页，不宣称来源全量跨页；官方原页完整 JD 仅标为可视核验，列表 `description` 仍是摘要。`0024_liepin_evidence_copy.sql` 只更新旧版精确文案，避免覆盖后续运行状态。

## 验证

- `npm run typecheck`：通过（renderer 与 Electron main）。
- 迁移清单、旧猎聘文案升级和来源目录三条针对性 pytest：3 passed；1 个既有 Starlette deprecation warning。
- touched Python/tests 的 Ruff：通过；`git diff --check` 与 `tasks.json` JSON 校验通过。
- 未重复全量测试，未重打包，未操作、关闭或重启当前 BOSS 登录窗口。

## 生产窗口状态与限制

调度者已在最终实际 `.app` 验证正常启动、进入来源页，并在右侧隔离视图打开 `zhipin.com/web/user` 的 BOSS 手机号/短信登录页。该证据只证明登录入口可达，不证明用户已登录，也不证明列表、详情、字段、分页或投递页能力。

D12c 改动存在于源码，按“不要打断登录”要求尚未打进当前运行包；应在用户完成/放弃本次登录协调后再统一构建，不得为本任务单独重启现有窗口。
