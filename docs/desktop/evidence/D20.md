# D20 本项目开发产物整理

2026-09-19，按用户“仅本项目、优先可恢复归档”的授权执行，D19完成后开始。未永久删除，也没有运行无关测试。

## 归档及整理

归档目录：`.development-archive/2026-09-19-d20/`（项目根目录下，已加入.gitignore）。[完整清单](../../../.development-archive/2026-09-19-d20/manifest.json)逐项记录原路径、去向、原因、文件数、逻辑/分配字节及SHA-256；执行脚本也保留在归档中。移动通过显式清单执行，没有通配批量删除。

- 117项归档，其中10份旧开发包：D14目录包、D15、D17中间与预览包、D18预览、D19最初预览及UI2/3/4/5。
- 归档已结束的临时辅助脚本、日志、原型副本、旧截图，以及项目内build/pytest/ruff/Python字节码缓存和本项目独立uv/PyInstaller缓存。归档逻辑内容共3,896,808,839字节，含目录分配空间约3.7 GiB；移动本身不释放磁盘。
- 21张有效UI6截图移入[evidence/D19](D19/)，保留D13–D18有效证据。旧D19/UI2截图不再散落当前证据根目录。
- ORCHESTRATION旧原文另存一份可恢复历史副本，现行文件只保留当前规则和历史索引；HANDOFF、桌面README、D14/D15/D17/D18/D19引用同步更新。最新入口仍是UI6。

## 保留与未动

保留 `/private/tmp/JobFindsMe-D19-UI6.app`、`apps/desktop/release/mac-unpacked/JobFindsMe.app`（既有发布目录用途不完全确定）、当前dist/dist-electron/runtime、.venv、node_modules、源码、配置、package-lock.json、uv.lock及必要UI原型。

所有数据库、简历、profile/user/data/smoke目录、登录分区和凭据原地保留，包括本轮合成验收数据；不把“测试目录”直接当成可删数据。用途不确定的jobfindsme-source-audit-20260918、d07-export/render、d13-ready等原地保留。具体留存路径见清单。

进程只读核对没有待归档旧.app运行；检测到项目外 `~/.jobfindsme/runtime/bin/python` 进程，未停止或移动它们，未访问正式数据和外部安装skill。本项目独立uv缓存锁无人持有，.venv无指向该/tmp缓存的符号链接。

## 验收

每项移动前后递归内容SHA-256、文件数、符号链接与逻辑字节一致，所有目标拒绝覆盖。227个源码/配置文件聚合哈希整理前后一致；整个UI6包哈希不变。归档没有跳过失败项，保留项从未进入移动清单。

验收使用清单/文件完整性及Markdown本地链接检查，未重跑业务测试或重新构建。当前项目状态仍未提交，不能把未跟踪源码视为垃圾。恢复时按manifest逐项移回原路径；若原路径已有新产物，先另存处理，不能覆盖。

最终核对61个Markdown本地链接全部存在，138个移动目标全部存在且旧位置全部消失；另有1份历史调度文档副本。归档目录分配空间3,923,419,136字节（约3.65 GiB），26个/tmp数据或不确定目录原地保留。随后用户新增D21/D22功能请求，归档完成状态不回退；新开发生成的产物将在其验收结束后再按同一边界收尾。
