# D01 浏览器与来源可行性验证

日期：2026-09-19。结论：猎聘匿名列表、跨页和完整原页 JD 已在真实生产包验证；三个需登录平台仍需用户真实桌面会话验收，本任务保持 blocked。

## 能力表

| 来源 | 登录 | 列表检索 | 完整 JD | 分页 | 投递页 | 当前处理 |
| --- | --- | --- | --- | --- | --- | --- |
| BOSS直聘 | 待真实登录验证 | 未登录禁用 | 待验证 | 待验证 | 待验证 | 桌面 API 返回 `login_required`，实时检索关闭 |
| 猎聘 | 匿名路径不要求登录 | 92 条真实列表记录 | 隔离原页显示完整职责/要求；列表仍诚实保留摘要 | 5 个桌面结果页，第 2 页稳定 | 官方原页可打开，未投递 | 匿名生产路径已验证 |
| 智联招聘 | 待真实登录验证 | 未登录禁用 | 待验证 | 待验证 | 待验证 | 桌面 API 返回 `login_required`，实时检索关闭 |
| 前程无忧 | 待真实登录验证 | 未登录禁用 | 待验证 | 待验证 | 待验证 | 桌面 API 返回 `login_required`，实时检索关闭 |

2026-09-19 调度者已在最终实际 `.app` 打开 BOSS 右侧隔离登录页，手机号、短信验证码与协议控件可见；没有代用户输入或登录。因此这里只把“登录入口可达”视为已验证，BOSS 会话与检索能力仍保持 blocked。

## 验证

- 真实 `.app`：使用合成简历和隔离用户数据目录，以 `Python` 检索猎聘，结果 92 条/5 页；第 2 页岗位集合变化。打开第 2 页首条官方职位页，完整职责与任职要求可见。
- 本地浏览器桥：`127.0.0.1:9222` 在探测时不可达，因此不能证明 BOSS/智联/前程无忧的登录、列表、详情、分页或投递页兼容性。
- 离线门禁：`pytest tests/desktop_api/test_app.py -q` 验证 BOSS/智联/前程无忧未有会话时 `live_search_enabled=false`，猎聘为匿名可尝试。

## 限制

未使用用户账号、Cookie 或验证码；没有点击职位页内的申请按钮，未提交任何申请。下次获得真实登录条件时，每个平台只做一次有记录的最小验证。

## 后续实现说明

2026-09-19 的 D04d 又补齐“登录后验证检索”入口，消除了登录来源永远无法从 disabled 进入 verified 的产品闭环；验证固定关键词、最多两页，并分别记录详情/字段/分页证据。代码闭环仍不替代用户真实登录，因此本任务继续 blocked。
