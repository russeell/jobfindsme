import test from 'node:test';import assert from 'node:assert/strict';
import {canonicalJobUrl,reportMatchesJob,hasFullDescription} from '../dist-electron/shared/research-reports.js';
test('reports belong to stable job identity or canonical detail URL, not company alone',()=>{const url='https://www.zhipin.com/job_detail/123.html';const report={job_id:'old-import',job_context:{url:url+'?securityId=old&utm_source=x'}};assert(reportMatchesJob(report,{job_id:'new-import',apply_url:url+'?ka=new'}));assert(!reportMatchesJob(report,{job_id:'other',apply_url:'https://www.zhipin.com/job_detail/456.html'}));assert.notEqual(canonicalJobUrl('https://careers.tencent.com/jobdesc.html?postId=1'),canonicalJobUrl('https://careers.tencent.com/jobdesc.html?postId=2'));});
test('structured list summaries are not labelled complete JD',()=>{const job={description:'岗位名；学历要求；工作地点',source:{source_name:'猎聘',detail_level:'structured_source'}};assert(!hasFullDescription(job));assert(hasFullDescription({...job,source:{...job.source,detail_level:'detail_page'}}));assert(!hasFullDescription({...job,description:'',source:{...job.source,detail_level:'detail_page'}}));});
