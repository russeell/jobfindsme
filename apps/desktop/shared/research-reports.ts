import type {ResearchReport,SearchResultItem} from './contracts';
const tracking=/^(utm_.*|ka|lid|securityid|sessionid|pgref|ckid|from|source|scene|timestamp|t|track_id)$/i;
export function canonicalJobUrl(value:string):string {try {const url=new URL(value);url.hash=['app.mokahr.com','campus.kuaishou.cn'].includes(url.hostname)?url.hash.split('?')[0]:'';url.hostname=url.hostname.toLowerCase();const detail=/\/(job_detail|job|a|jobdetail)\//i.test(url.pathname);for(const key of [...url.searchParams.keys()])if(tracking.test(key)||(detail&&/zhipin\.com$|liepin\.com$|zhaopin\.com$/.test(url.hostname)))url.searchParams.delete(key);url.searchParams.sort();return url.toString();}catch{return value.trim();}}
export function reportMatchesJob(report:ResearchReport,job:SearchResultItem['job']):boolean{return report.job_id===job.job_id||!!report.job_context?.url&&canonicalJobUrl(report.job_context.url)===canonicalJobUrl(job.apply_url);}
export function hasFullDescription(job:SearchResultItem['job']):boolean{return !!job.description.trim()&&(job.source.detail_level==='detail_page'||job.source.source_name==='用户确认 JD（未独立核验）');}
export function reportStatus(report:ResearchReport):string{return report.outcome==='failed'?'检索失败':report.outcome==='no_evidence'?'暂无公开证据':report.status==='limited'?'部分结果':'已完成';}
export function reportJob(report:ResearchReport):SearchResultItem['job'] {if(!report.job_id)throw new Error('公司研究没有岗位快照');return report.job_snapshot??{job_id:report.job_id,title:report.job_context?.title||'历史岗位',company:report.job_context?.company||'公司未知',description:report.job_context?.description||'',apply_url:report.job_context?.url||'',locations:report.job_context?.locations||[],salary_min_k:null,salary_max_k:null,experience_min_years:null,experience_max_years:null,recruitment_track:'unknown',employment_type:'unknown',source:{source_name:'历史报告快照',liveness:'unknown'}};}
