# D57 来源登录会话延续

2026-09-25。用户反馈猎聘、前程无忧在同一 .app 重启和换新版 .app 后都要求重新登录，并询问 16 家公司官网。

- 20 个来源原本均有独立的 `persist:jobfindsme-source-*` 分区，包括全部 16 家公司官网。旧测试包的 `previewUserData` 随 D 编号变化，换包就换 Chromium 资料目录；本次打包固定沿用现有 D55 资料目录，QA 仍可用 `JFM_PACKAGE_PREVIEW_USER_DATA` 或启动参数隔离。未复制、读取 Cookie 值或改动旧目录。
- 猎聘、前程无忧的来源按钮改从官网首页进入，保留原登录 URL 供来源契约使用；不再每次主动导航到登录页。完整退出前调用已使用来源 session 的 `cookies.flushStore()`；关闭窗口仍保持原有隐藏行为。
- `node --test tests/browser-tabs.test.mjs tests/source-browser-policy.test.mjs`：32 passed，覆盖两个入口、退出刷写及全部 16 家公司入口。完整桌面 Node 测试在允许回环监听的环境中 83/83 passed；TypeScript 构建、普通包审计通过。
- 新包 `/private/tmp/JobFindsMe-D57-Session.app`，包内 `previewUserData=jobfindsme-preview-D55-reading-column` 与 D55 包相同，构建标签不同。隔离原生进程在全新 `/private/tmp` 资料目录中设置合成持久 Cookie，完全退出并重启后，猎聘、前程无忧、腾讯分区均保留 1/1；合成 session-only Cookie 均为 0/1。这验证持久会话的应用链路，也明确网站若仅发本次会话 Cookie，退出后仍会要求登录。测试目录已删除，未访问真实招聘站点。

限制：未使用用户账号验证两个平台的真实登录；16 家公司仅核对独立持久分区及入口，未逐站登录。来源能保留 Cookie 不代表平台仍接受登录，也不代表岗位列表、详情或续页可检索。旧 D29 等其他测试资料目录中的账号数据未迁入 D55；若前程无忧只在旧目录登录过，需在新包内手动登录一次。网站自行过期、撤销或仅设置 session-only Cookie 时，应用不延长网站设定的有效期。
