# 当前状态

更新于 2026-09-25。状态以 [任务清单](tasks.json) 为准；这里仅保留当前需要处理的事项。

当前 GitHub 文档目录只保留开发入口、状态、结构、任务清单和两张 README 图片。旧计划、原型与逐任务证据可从固定历史提交查阅。按用户确认，旧 CLI/MCP、安装器、Skill 和旧发布流程已退役；桌面检索、数据迁移和当前测试保留。

## D64 研究 Agent 实施边界

沿用 Pi 的单一模型/工具循环、现有密钥存储、受限浏览器、研究服务与报告存储；项目侧掌管对象识别、工具权限、预算、证据质量、引用校验、报告版本和工作区隔离。已将 `search_public_evidence` 一次调用即保存报告的路径替换为 `find_evidence`、`search_web`、`read_page`、`read_job`、`read_browser_page` 工具适配；原文读取失败后才允许隔离浏览器回退。搜索摘要不得作为证据。普通聊天不启用网页工具，也不自动生成报告。

本地 D64 验收：研究页只有问题输入、模型选择、发送/停止，明确公司无需岗位链接，歧义问题在对话中追问。SQLite 将对话、执行与报告分别保存并按工作区隔离；执行轨迹含预算、证据和失败，取消/失败不覆盖旧报告。研究模型须返回陈述、原文连续引句、证据 ID、类别和范围；前后端共同拒绝无效证据、主体/否定/时间/地区/数字/上市状态不符及明显过度归纳。展示区标注直述或限定归纳、原文、证据 ID、来源类型和适用范围。相同原文上的实质陈述变更也生成新版本；普通追问不自动生成报告。此校验是保守的文本支持筛查，不能替代真实语义核验。Python 定向 10 项、Node 全套 94 项通过（含本地模拟模型的 Pi 多工具与普通追问链路）；此前 Python 全套 372 项及两个回环测试通过。隔离原生包取得默认、860px 窄窗、600px 矮窗、历史重开、无依据回答和追问截图，均为明确标记的合成数据。真实付费模型、真实研究质量未验证，D64 保持进行中。测试版为本地 `outputs/JobFindsMe-D64-Review.app`，未推送或发布；截图与条件见 `outputs/D64-QA.md`。

2026-09-25 正确性补修：来源后续页失败时保留已读岗位并标记部分结果；历史对话完整存储，重开恢复岗位/公司上下文，切换会话时只接收当前会话流；问题上限统一为 700 字。检索运行绑定岗位版本快照，旧运行无法确认原快照时明确标记且不展示旧分数。研究执行记录关联会话，区分原文证据与最终回答；搜索预算收紧，原文证据 ID 由读取内容生成；取消后可立即重试。补修后 Python 全套 388 项、Node 全套 103 项及合成/金标准评估门槛通过，桌面构建通过；Windows CI 增加时区依赖及 UTF-8 设置，尚未在 Windows 实机运行。原生界面与真实模型仍沿用上述验收边界。

同日复核补丁：15 轮且单条回答超过 8000 字的完整对话继续存储，发送前仅裁剪模型上下文，并用主进程实际采用的 IPC 校验函数验证可继续追问。退出应用先中止活动研究会话，AbortSignal 传至 Pi 与后端请求。搜索工具、桌面 IPC 和 Python 检索接口的 700 字边界一致，原始问题与模型检索词分字段传递，不暗中截短。复核后 Python 全套 389 项、Node 全套 107 项、桌面构建通过；未运行真实付费模型或 Windows 实机。

存储续修：SQLite 会话写入现在按最多 1000 条、单条 10 万字及总 JSON 16 MB 显式校验，模型 IPC 仍只接收最近最多 200 条且受字符预算约束；完整会话不会按模型预算截短。前端 `toStored` 样本经后端保存端点写入 220 条消息，重建应用后逐条读回一致，其中前 15 轮助手回答都超过 8000 字；旧设备缓存迁移改为写入后读回核对，失败保留缓存并显示原因。续修后 Python 全套 391 项、Node 全套 109 项、桌面构建通过。

UX 续修：研究历史改为正文外的独立滚动面板，Esc 关闭并将焦点返回历史按钮；打开与关闭保留报告阅读位置，切换会话才回到起点。报告按回答轮次呈现一次，助手原文和报告 ID 仍完整保存在 SQLite；旧会话按问题关联原有报告，普通对话继续显示文字。来源文案区分读取与事实核验。默认窗口的合成原生检查已覆盖历史面板、焦点、阅读位置和旧报告；Node 110 项、桌面构建和独立 macOS 包审计通过。860px 有内容报告与历史面板截图、Esc 焦点复位及最终包启动复核也已完成；真实来源和模型质量未据此验收。独立应用为 `outputs/JobFindsMe-UX2-Review.app`，未推送或发布。

UX3 与来源边界续修：普通对话保留自然回答；“想了解 Agent 相关岗位”直接给现有找工作页的预填关键词，随后输入公司名延续为“公司 + Agent”，由用户选择来源后才检索。无证据回复按未检索、搜索错误、原页读取失败、主体不符和验证/限流分别说明。研究顶栏删除重复的“找工作/返回找工作”，侧边栏含窄窗折叠态的“找工作”导航保留。研究来源新增公开网页发现、官方优先与改写查询的模型指引；所有搜索站点共用 Bing RSS，`web` 不是独立搜索商。服务失败后停止该供应商的全部搜索，仅允许直达已保存的核验原页或已保存岗位的精确 URL；不得猜测官网。原文读取支持有文字层且不超过 2 MB/40 页的 PDF，按页引用；搜索错误和空结果分别记录，读取遇验证/限流按实际域名停止，其他公开域名仍可读取。扫描 PDF 无文字层单独标记 `no_text_layer`；已保存岗位区分关闭、过期、未知、最近观察，最近观察不等于当前在招。复核后按简单/综合问题分别限制搜索 2/4 次、原页 4/10 次、模型 5/7 轮、总时长 45/75 秒；每次来源请求接收剩余时间，截止或取消中止前端正在进行的请求，Python 来源连接超时最多 4 秒。复核合成回归：Python 398 项、Node 121 项、构建与独立包审计通过；固定样本原文取得 2/2、可判定主体 3/3、引文支持判定 3/3，岗位实时有效 0/5 经核验（均不计成功），模拟模型费用不可评估，未使用真实付费模型或批量站点。独立评审包为 `outputs/JobFindsMe-UX3-Review.app`，新的预览数据目录，不覆盖 UX2。UX3 界面布局先前已在 1100×800 原生窗口检查空态、三轮对话、移除重复顶栏按钮、预填搜索但不自动检索并取得截图；本次来源逻辑续修改包后尚未重新做原生窗口复验。860px 和 600px 验收时 Mac 被锁定，尚待解锁后继续；真实来源研究质量仍未验证，详见 `outputs/UX3-Review.md`。

UX4 实际故障复核：用户 UX3 截图显示“腾讯怎么样”落入无证据模板且对话保存提示本地服务异常。隔离包复现发现 Bing RSS 从 `www.bing.com` 区域跳转至 `cn.bing.com` 被同主机规则拦截；本机环境代理还造成 TLS 校验失败。现在研究检索仅允许这两个 Bing 主机之间的 HTTPS 安全跳转，研究网络请求使用 certifi 验证并不继承环境代理；限定单次真实查询取得 6 个候选。旧 `research_executions` 表只允许 4 种状态，与代码的 `search_service_error` 等扩展状态不符；0038 迁移保留旧记录并扩大 CHECK。保存对话的具体 UX3 瞬时错误无法从运行中实例取得：原实例未中止也未写入其数据库；独立旧包复测新增对话成功、失败执行写入返回 500。新包为聊天 SQLite 错误返回安全原因代码，对 `sqlite_busy` 重试一次。宽泛“腾讯怎么样”先追问经营、岗位或体验范围，回答后继续研究；证据校验仍不接受未引用的当前事实。Python 406 项、Node 124 项、构建和独立包审计通过。UX4 独立包在隔离数据库中完成对话新增/更新/重启读回、失败执行、报告保存/重开和单次公开检索；原生窗口检查了范围澄清与本地保存。真实付费模型未调用，真实研究结论质量仍未验证。包和细节见 `outputs/UX4-Review.md`。

UX5 内嵌 Pi 统一对话：前端仍识别链接及保留显式“去找工作”入口；其余普通聊天、宽泛问题澄清和公司研究现在进入同一个 Pi Agent 模型/工具循环。无公司时 Pi 只可用 `select_subject` 选择当前问题中明示的公司，随后才能查已存证据和有界公开来源。研究输出仍须通过原文引句、证据 ID、主体及范围校验；无证据时仅接受模型的短澄清问题，事实缺口由执行轨迹说明，不能把模型常识变成实时结论。取消后保留原问题，下一次可直接重试。Node 127 项、Python 406 项通过；独立 UX5 包审计通过，包内 Python 服务的隔离保存/重启/单次搜索烟测通过。原生 UX5 应用连接本机模拟模型，连续验证“你是谁”自然答复、“腾讯怎么样”由 Pi 澄清、补充“经营”后 Pi 调用 `find_evidence → search_web → read_page`、无受支持陈述不生成报告、停止延迟请求后重试且无重复用户消息；隔离数据库确认工具轨迹和对话轮次。真实付费模型未调用，原 UX3 活跃实例及其记录未修改；详见 `outputs/UX5-Review.md`。

验收以合成模型及网页检查：无链接明确公司、歧义追问、岗位/JD 与链接问题、追问复用/切换主体、无结果/过期/冲突/受限、网页注入不越权、引用存在但不支持关键结论、预算与取消、失败保留旧报告，以及对话/执行/报告重启与工作区隔离。原生检查覆盖默认、窄窗、短窗、长输入和浏览器分栏；未取得真实模型、登录或计费授权的能力如实列为未验证。本轮仅本地提交，不推送或发布。


UX6 设置页布局：删除正文的返回按钮、重复设置标题和来源大标题，三个设置分类移入固定顶栏；来源摘要、分类与全部检查合并为一处工具区，来源卡片收紧间距；模型设置保留安全存储状态、计费提示、配置、高级选项及连接管理。桌面构建和独立包审计通过；隔离原生窗口在 900×600 与 760×600 检查了三个分类、来源勾选、能力限制折叠、键盘 Tab/回车与模型表单，未触发模型测试或来源检查。截图与限制见 `outputs/UX6-Settings-Review.md`。独立交付包使用 `jobfindsme-preview-UX6-settings-delivery`，未接触原有实例。

- **D55 · 原生界面复验进行中**：教育经历分组与年份、来源选择、研究阅读列已实现。默认及窄窗截图已有局部证据；最终原生双教育/浏览器分栏复验仍待完成。
- **D62 · Pi 研究对话已实现**：桌面主进程接入固定版本 Pi Agent 与模型协议；D64 已将旧的一次检索工具替换为分步研究工具。研究页支持取消、历史对话和报告回看。模型设置采用服务、密钥、模型 ID、保存并测试、设为当前模型的流程。隔离桌面测试版以本地模拟 OpenAI 兼容服务完成连接、普通对话、历史恢复与工具调用验证；D64 合成模型多工具回归通过，未使用付费密钥或验证真实模型质量。
- **D63 · 20 来源逐站复验进行中**：2026-09-25 在隔离桌面测试版逐站触发检查。腾讯、字节、阿里、美团、百度、京东、网易、快手、小米、拼多多、DeepSeek、MiniMax、智谱、月之暗面、猎聘及前程无忧此前返回可检索状态。前程无忧读取 2 页、40 条，列表、字段、续页已验证，完整详情仍部分可用；阿里完整详情也未确认。D64 隔离版对滴滴、阶跃星辰各作一次有界复测：滴滴仍提示来源无法读取，阶跃星辰仍提示检索未完成。阶跃星辰 SPA 入口异常处理和错误分类修复后，在新打包的独立 900×600 QA 应用中单独重试一次，界面仍返回“本次检索未完成，本地服务暂时异常”；本轮未取得可验证岗位列表，保留历史状态且不计成功。此后未再发起网站请求；具体失败来源仍待诊断。BOSS 与智联此前要求重新登录，用户原有应用会话未被改动。网站续页与城市覆盖不因一次列表读取而视为完全验证。
- **D01 · 两个平台真实登录阻塞**：猎聘匿名检索、前程无忧当前列表读取已在隔离桌面版验证；BOSS、智联需要用户在应用内完成登录后，才可验证列表、详情、字段和续页。官网能打开不等于自动检索可用。
- **D38 · 来源局部验证阻塞**：智联仍待登录会话；阿里及前程无忧完整详情仍部分可用。其他来源按应用内能力状态显示，不能笼统宣称 20 个来源均可检索。
- **D04、D12 · 最终来源与发行验收待办**：依赖真实来源能力和发行检查。定时检索保持停用；旧数据与迁移保留。

D57 已修复猎聘、前程无忧会话跨重启延续，16 个公司官网使用各自独立的持久分区。网站会话过期或主动验证仍可能要求重新登录。

### D63 本轮逐站记录

以下为 2026-09-25 在隔离测试版逐个点击“重试检查”的结果；“可检索”只表示该次有界读取返回岗位，未验证全站覆盖。状态保存在隔离测试工作区，不写入用户原有会话。

| 来源 | 本轮结果 | 已知限制 |
| --- | --- | --- |
| BOSS直聘 | 需重新登录 | 未验证本轮列表、详情、续页 |
| 猎聘 | 可检索，部分覆盖 | 匿名路径；其余能力以应用状态为准 |
| 智联招聘 | 需重新登录 | 未验证本轮列表、详情、续页 |
| 前程无忧 | 可检索，2 页 40 条 | 列表、字段、续页已验证；完整详情部分可用 |
| 腾讯 | 可检索，2 页 40 条 | 列表、详情、字段已验证；网站续页部分覆盖 |
| 字节跳动 | 可检索，1 页 11 条 | 列表、详情、字段已验证；网站续页部分覆盖 |
| 阿里巴巴 | 可检索，1 页 10 条 | 列表、字段已验证；完整详情及网站续页部分覆盖 |
| 美团 | 可检索，1 页 10 条 | 列表、详情、字段已验证；网站续页部分覆盖 |
| 百度 | 可检索，1 页 5 条 | 列表、详情、字段已验证；网站续页部分覆盖 |
| 京东 | 可检索，1 页 20 条 | 列表、详情、字段已验证；网站续页部分覆盖 |
| 网易 | 可检索，部分覆盖 | 详情、续页未单独扩展核对 |
| 快手 | 可检索，部分覆盖 | 详情、续页未单独扩展核对 |
| 小米 | 可检索，部分覆盖 | 详情、续页未单独扩展核对 |
| 滴滴 | 两次读取失败 | 只显示历史可检索，不能计为本轮通过 |
| 拼多多 | 可检索，部分覆盖 | 详情、续页未单独扩展核对 |
| DeepSeek | 可检索，部分覆盖 | 详情、续页未单独扩展核对 |
| MiniMax | 可检索，部分覆盖 | 详情、续页未单独扩展核对 |
| 智谱 | 可检索，部分覆盖 | 详情、续页未单独扩展核对 |
| 月之暗面 | 可检索，部分覆盖 | 详情、续页未单独扩展核对 |
| 阶跃星辰 | 修复前两次、修复后新包一次均提示本地服务异常 | 未取得本轮岗位列表；只显示历史可检索，不能计为通过 |

预览版“检查全部来源”受单次实时探测上限约束。本轮逐项检查绕过了该上限；界面汇总文案现区分“已处理”和“实际探测”。

过去的逐任务记录可在 [2026-09-25 归档提交](https://github.com/russeell/jobfindsme/tree/a3a714e17ea73adabd54d36821c46ebc8a924461/docs/desktop/evidence) 查看。后续开发只更新上述当前状态与任务清单，不再向当前仓库树添加逐任务历史文档。
