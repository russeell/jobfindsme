# D39–D41 验收记录

日期：2026-09-23。交付版本：`D41-20260923-ui30`。

## 简历维护（D39）

- 主入口收敛为导入、逐条核对、确认当前版本、手动维护、历史恢复与 PDF/DOCX/MD 导出。主页面不再展示对话补丁、模型选择、按 JD 改写；历史版本和原始文件仍保留。
- `deterministic-resume-v4` 识别同一行的中文段落标题。UI30 隔离包导入 6 行 TXT 样本，得到 2 条技能、1 条教育、1 条工作、1 条项目事实；姓名未当作事实，技能行未混进项目。
- 原生窗口确认导入生成 v1，修改项目经历生成 v2，从历史恢复 v1 生成 v3。原 v1/v2 仍可见，当前 v3 内容与 v1 一致。预览中可见三个导出格式入口；本轮没有逐一写出三个文件。

## 岗位研究（D40）

- 自定义问题先于快捷方向用于有限公开检索，并存入报告岗位快照；无自定义问题时使用快捷方向。结果先显示已保存报告，调整问题与再次研究折叠在后。粘贴链接解析后先确认岗位并设置问题，再发起研究。
- 后端测试核对了问题进入查询、报告留存、公司只出现在页脚时不误认口碑证据，以及没有证据时的未查明说明。没有用真实用户岗位做新的外网口碑报告。

## 来源与智联登录（D41）

- 已在 Google Chrome 同一官方登录链接确认手机号、短信验证码表单。UI23 内嵌页最初只显示宣传图和页脚。临时诊断记录到 `XMLHttpRequest.setRequestHeader` 的非 ISO-8859-1 字符错误，同时确认 Electron 默认 User-Agent 含中文应用名；将智联前台原页 User-Agent 规范为 ASCII 后，冷启动表单恢复。请求头的具体站点脚本未进一步逆向；临时日志未进入 UI30。
- UI29 冷启动的全新隔离资料目录里，点“岗位来源 → 智联招聘 → 打开来源/登录”直达用户提供的 `passport.zhaopin.com/login?bkUrl=...`，原生无障碍树立即出现手机号、短信验证码、获取验证码、协议勾选和登录按钮；UI30 同代码保留此修复。没有输入账号或验证码，因此登录会话、列表、详情和网站续页均未验证。
- 来源卡片默认窗口为 2 列，约 860px 窄窗为 1 列；移除了重复的检索状态文案。登录、列表、详情和网站续页状态分开记录。已加载的智联/前程无忧页面只做被动 DOM 观察；看到可读列表后才允许一次有冷却期的有界检索。安全验证立即停源。四平台与 16 家公司各自能力和历史证据见 [来源矩阵](D41/source-matrix.json)。矩阵保留 D37/D38 的有界样本日期，未把历史样本写成当天重新核验。
- 发现页来源采集改为最多两个来源同时运行，同来源后台检索串行；界面展示首个来源完成时间、各来源耗时和候选数。受阻来源保持部分结果与限制说明。

## 验证与边界

- Python 定向测试：17 项通过；桌面来源定向 Node 测试：34 项通过；ruff、前端类型检查与构建、Python runtime 构建、两份 `.app` 包审计通过。
- UI30 原生默认/窄窗检查覆盖来源布局和简历导入、维护、恢复。智联表单使用 UI29 新隔离目录冷启动核验。口碑报告的真实外网采集、智联登录后的自动搜索和 20 来源当天全量实测仍待各自有界验证。
- 用户 D38 测试版与既有会话保持运行；隔离包使用 `jobfindsme-preview-D41-qa30`。交付包 `/private/tmp/JobFindsMe-D41-UI30.app` 沿用既有默认用户资料目录，未启动它以免触碰用户会话。
