# D47 目录归组验收

2026-09-23，范围与逐文件映射见 [STRUCTURE](../STRUCTURE.md)。本轮唯一写入者，原基线 9c1f64c。

## 阶段 1：路径迁移

- Python 11 个模块、Electron 13 个模块、renderer 15 个模块纯路径迁移（39 个文件）；core 空包初始化文件删除，新建 search/sources 包初始化。
- desktop_api/app.py、main/index.ts、App.tsx 仅更新 imports；不拆分业务，不改 API、数据表、来源行为、账号分区或模型调用。
- 共享导航策略移至 desktop/shared，renderer 不再间接依赖 main 源文件。
- 构建先清除本项目生成的 dist-electron，避免旧路径编译产物混进安装包。
- Python 全量回归初跑：467 passed，2 个 localhost 测试被沙箱 EPERM 阻止，1 个既有迁移断言缺少已存在的 0032；补齐断言后两项回环测试及 storage 共 12 passed。未改变迁移 SQL。
- 原默认 pytest 收集存在 test_app/test_service 同名冲突；改 importlib 收集方式及两处测试 helper 的绝对导入。470 tests collected，不删测试规避失败。
- Node 76/77 首轮通过，唯一失败为 localhost 监听 EPERM；获工具权限后 discovery-feedback 5/5 通过，覆盖原失败项。全部 77 个用例已通过。
- npm run build 通过；清理后的 dist-electron 只含新路径，根启动入口不变。
- 改动模块 Ruff 导入排序校正，迁移文件中既有长行仅换行。

阶段 1 已完成，后续验收见下文。未进行真实来源检索、登录或付费模型调用。

## 阶段 2：有限清理与文档

- 删除 5 个旧插件市场 JSON 清单（逐文件见 STRUCTURE）；删除仅检查这些清单存在/版本的一个测试，其余安装/CLI/MCP/Skill 测试保留。
- 4 份旧入口文档归入 docs/legacy，根中英文 README 改为桌面产品入口；更新相对链接，旧插件市场宣传撤下。AGENTS 和项目内开发 skill 同步当前路径；全局 skill 未动。
- 兼容安装测试改为检查新的 legacy 文档路径，断言内容保留。canonical Skill 同步检查通过，0.11 数据库旧薪资载荷 smoke 通过。
- 未删除迁移、有效脚本/CI、锁文件、许可证、连接器、历史证据、简历编辑/历史兼容，也未读写用户数据库或登录 profile。

## 阶段 3：打包与隔离验收

- `npm run build:python-runtime`：PyInstaller arm64 目录运行时成功重建；归档含新 search/sources 模块，不含 11 个被移动模块的旧路径。
- 32 份 SQL 迁移与 3 份 JSON/JS/Markdown 资源在 frozen runtime 中逐字比对一致。Git 中迁移文件无改动。
- 11 个被移动的 Python 模块去掉 import 节点后 AST 与 9c1f64c 完全一致；无业务重写。
- `npm run build` 成功。普通包 `/private/tmp/JobFindsMe-D47-Structure.app` 与预览包 `/private/tmp/JobFindsMe-D47-StructureQA.app` 均通过 `audit:mac:dir`；审计新增新模块必需项与旧编译路径拒绝项。
- CUA 仅启动预览包，窗口标题显示 D47 / 隔离测试，独立目录 `jobfindsme-preview-D47-structure`。本地服务成功就绪（1 个新工作空间），找工作和口碑调查入口均正常渲染；随后读取简历空态和匹配规则默认值均成功。没有导入真实简历、账号登录、来源检索或模型请求。
- [找工作截图](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D47/search.jpg)、[口碑调查截图](https://github.com/russeell/jobfindsme/blob/1beb7dbe4c465ade47452ae706de487a3582b0eb/docs/desktop/evidence/D47/research.jpg)。截图只证明入口和本地服务加载，不证明在线检索。
- 使用原生应用菜单退出预览；应用清单确认 D47 已退出，既有 `com.jobfindsme.desktop` 用户实例仍运行。普通交付包未启动。
- Synthetic evaluation 成功；golden_v1 gate PASS（P@20=1.0，NDCG@10=1.0，Recall@20=0.833）。这仅是既有离线基准。

## 汇总和限制

- 本地阶段提交：`687a0e7` 路径迁移；`cd007fb` 有限清理与文档；打包证据另随最终提交。
- 39 个源码文件迁移（Python 11 / Electron 13 / renderer 15），4 份旧入口文档归档；删除 5 个插件 JSON 清单和 1 个空包初始化文件。没有删除业务回归测试文件；仅移除已退役清单的专属测试函数。
- 迁移阶段 470 个 Python 用例经全量与失败项定向补测覆盖通过；当前移除一个市场清单测试后收集为 469。兼容链阶段 49 项经 48+1 定向修复验证通过。测试 helper 导入调整后 11 项通过。Node 77 个用例经 76+回环失败项补测通过。不是声称一次无沙箱限制全量运行全绿。
- Ruff 校验迁移模块及改动的导入通过；未对全项目历史格式做美化。文档相对链接检查通过。
- CLI/MCP 外部命令、资源与现有 CI 保留；内部 Python 模块路径已改变，仓库内导入全部迁移。若外部用户直接导入这些内部模块，需要按 STRUCTURE 映射调整，不增加重复 shim 文件。
- importing/repository 的存储依赖、既有浏览器管理类中的采集协调、入口中的 IPC 组装和 retired resume prompt API 保持原状；独立解耦/退役应另立任务，不能从 UI 已移除推断历史 API 可直接删除。
- D38/D41 真实来源阻塞与旧证据限制仍成立。本轮无新增在线来源验收、签名/公证、推送或发布，不改变用户活跃应用和数据。
